import { IconButton } from '@components/Buttons';

function NewChatLogButton() {
  return (
    <IconButton
      icon="ic:round-plus"
      buttonColor="transparent"
    >
      새로운 타로점 보기
    </IconButton>
  );
}

export default NewChatLogButton;
