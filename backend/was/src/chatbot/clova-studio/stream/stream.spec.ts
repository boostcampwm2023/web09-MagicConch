import {
  apiResponseStream2TokenStream,
  createTransformStream,
  createTransformer,
  extractKeyValue,
  getTokenExtractor,
  isStreamEvent,
  streamEventParse,
} from './converter';

describe('[chatbot/clova-studio/stream]', () => {
  describe('function convertAPIResponseStream2TokenStream()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function createTransformStream()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function createTransformer()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function extractKeyValue()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function getTokenExtractor()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function isStreamEvent()', () => {
    it('test', () => expect(false).toBe(true));
  });

  describe('function streamEventParse()', () => {
    it('test', () => expect(false).toBe(true));
  });
});
